namespace Sandbox.Graphics.GUI
{
    using System;

    public enum MyGuiControlHighlightType
    {
        NEVER,
        WHEN_CURSOR_OVER,
        WHEN_ACTIVE,
        FORCED,
        CUSTOM
    }
}

